# ── Test overrides ────────────────────────────────────────────────────────────
# Disable the SQS consumer thread so @SpringBootTest context loads don't attempt
# to connect to a real (or LocalStack) SQS endpoint during test runs.
app.messaging.consumer.enabled=false
app.cors.allowed-origins=http://localhost:3000

# Dummy values — AWS clients are created lazily; no real connections are made
# during a context-only test (AuthApplicationTests).
app.messaging.otp-email-queue-url=http://localhost:4566/000000000000/otp-email-queue
app.messaging.ses-sender-email=test@example.com
cloud.aws.region.static=us-east-1
cloud.aws.credentials.access-key=test
cloud.aws.credentials.secret-key=test
cloud.aws.endpoint=http://localhost:4566
