apiVersion: v1
kind: ServiceAccount
metadata:
  name: auth-service-sa
  namespace: shop-platform
  labels:
    app: auth-service
automountServiceAccountToken: false  # Banking standard: never auto-mount unless explicitly needed
